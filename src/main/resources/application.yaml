spring:
  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/thechowloungedatabase?createDatabaseIfNotExist=true
    username: root
    password: password

  security:
    user:
      name: user
      password: 123